# 2026-05-19 引き継ぎ・進捗状況

## 現在の大方針

疎結合化は一旦保留している。現在の主目的は、ERIE-on-Lenia を「TRM の事前学習データを生成し、その後 TRM が外部世界と関わるための下地」として実用可能にすること。

重要な整理は次のとおり。

- `autopoiesis / 意識 / 主体が立ち上がるフェーズ` は、Lenia の挙動そのものから直接主張しない。
- Lenia はまず `z_lenia`、つまり intrinsic CA dynamics として扱う。
- Lenia の挙動は TRM の事前学習データセットになる。
- 主体性や autopoiesis を構成するのは、TRM が外部世界とどのような関わりの中で何をするかである。
- したがって現段階では、Lenia をどう走らせ、どの外部状態と結び、どのログを取るかを優先する。

## 正規語彙

外部状態は `resource / hazard / shelter` ではなく、次を正規語彙にする方針で統一済み。

- `energy_gradient`
- `thermal_stress`
- `toxicity`
- `niche_stability`
- `flow`

互換性のため、旧語彙は alias として残す。

- `resource` -> `energy_gradient`
- `hazard` -> `0.6 * thermal_stress + 0.4 * toxicity`
- `shelter` -> `niche_stability`

詳細は `2026-04-26_外部状態語彙統一.md` を参照。runtime / summary / dataset では正規 contact 指標を優先しつつ、旧指標も互換用に残す方針。

## 実装済み

### AdaptiveController

`trm_pipeline/adaptive_controller.py` を追加済み。

runtime への統合も済み。

- `trm_pipeline/erie_runtime.py`
- `run_episode()`
- CLI フラグ群

主な機能:

- episode 実行中に短い window metrics を読む。
- homeostatic error, policy entropy, external-state contact を使って調整する。
- runtime パラメータと Lenia `m/s` を安全範囲内で更新する。
- 外部状態場そのものは書き換えない。
- `*_parameter_history.json` を出力する。
- summary に `adaptive_controller`, `adaptive_event_count`, `final_lenia_params`, `final_runtime_config` が入る。

注意:

- これは CLI runtime 内の interval 更新としては機能している。
- viewer や外部インターフェースと常時接続して操作する意味での `live adaptive controller` は、まだ本格実装していない。

### Lenia parameter update

`LeniaERIEEnvironment.set_lenia_params()` を追加済み。

これにより adaptive controller や generation harness から Lenia の `m/s` を更新できる。更新時には kernel / species kernel を再構成する。

### GenerationHarness

`trm_pipeline/generation_harness.py` を追加済み。

主な機能:

- genome を生成する。
- candidate を episode 評価する。
- fitness を計算する。
- elite を選択する。
- mutation と inheritance を行う。
- 世代ごとに結果を保存する。

現段階の genome:

- runtime genes:
  - `beta_pi`
  - `contact_w_energy`
  - `contact_w_thermal`
  - `contact_w_toxicity`
  - `contact_w_niche`
  - `aperture_gain`
  - `lookahead_discount`
- Lenia genes:
  - `m`
  - `s`

出力:

- `generation_summary.json`
- `generation_candidates.jsonl`
- `selected_genome.json`
- `generation_*/candidate_*/<regime>/episode_*`

### Environment curriculum

`trm_pipeline/environment_curriculum.py` を追加済み。

regime:

- `balanced`
- `easy`
- `resource_rich`
- `sparse_energy`
- `toxic_band`
- `thermal_stress`
- `unstable_niche`
- `hard`

GenerationHarness は `--environment-regimes` を受け取り、candidate を複数環境で評価できる。

選択は平均 fitness だけでなく、`worst_regime_fitness` を含む `generalization_score` を使う。単一環境への過適応を抑えるため。

### dataset / training dry run 周辺

小中規模の生成と training dry run は試験済み。

関連して、以下のような契約が入っている。

- canonical external-state config
- role-specific view manifest
- `TRM-Wp / Bd / Bp / Vm / As` 向け manifest
- intervention data
- action-conditioned transition
- dataset gate
- production / agentic preset での external state gate

ただし、TRM を実運用レベルで十分に学習させる段階ではまだない。現状は「生成・契約・dry run が通る」段階。

## 検証済み

前回までに以下を確認済み。

- AdaptiveController の unit test
- GenerationHarness の unit test
- Environment curriculum の unit test
- ERIE runtime 関連テスト
- generation curriculum smoke run
- 小中規模の実生成
- training dry run

記録上、関連 broad tests は `110 passed` まで通っている。

代表コマンド:

```bash
./.venv/bin/python -m trm_pipeline.generation_harness \
  --output-root artifacts/generation_curriculum_smoke \
  --seed-catalog data/lenia_official/animals2d_seeds.json \
  --generations 1 \
  --population-size 2 \
  --elite-count 1 \
  --episodes-per-candidate 1 \
  --steps 6 \
  --warmup-steps 1 \
  --seed 20260513 \
  --mutation-scale 0.04 \
  --environment-regimes easy hard \
  --adaptive-interval 2 \
  --adaptive-window-size 2 \
  --adaptive-learning-rate 0.08
```

注意:

- テストや実行は `.venv/bin/python` を使うこと。
- global Python では torch / numpy の不整合が出た記録がある。

## 現在の外部環境の位置づけ

現状の外部環境は、まだ「世界そのもの」ではなく、ERIE が関わるための最小構成の外部状態場である。

持っているもの:

- energy source
- thermal stress
- toxicity
- niche stability
- flow
- body / boundary / aperture を介した contact
- action による接近・回避・封止・再構成
- contact に基づく viability / policy score
- regime による環境差

まだ弱いもの:

- 空間的な記憶
- 予測と実接触のズレ
- 外部状態の時間的因果性
- agent が環境を変え、その変化が後の観測に戻る経路
- 他個体・障害物・消費・枯渇などの相互作用
- 外部インターフェース経由で現実の外部世界に接続する層

哲学的には、現状は「界」と呼べる最小条件の一部を持つが、十分な `Umwelt` ではない。場があり、境界を介した contact と action はある。しかし、意味ある affordance、履歴、予測誤差、可逆/不可逆な環境変化、複数 episode をまたぐ適応がまだ薄い。

## 次に実装する予定だったこと

直近の作業は「現時点の実装と折り合いをつけながら外部環境の構築を進める」こと。

最初に入れる予定だった Phase 1 は次。

### 1. Spatial memory

ERIE runtime に局所的な外部状態記憶を追加する。

候補フィールド:

- `spatial_memory_energy`
- `spatial_memory_hazard`
- `spatial_memory_niche`

期待する挙動:

- body / boundary が接触した場所に記憶を deposit する。
- 記憶は decay / diffusion する。
- policy score が現在 contact だけでなく、過去に接触した場所の履歴も参照できる。
- snapshot / npz / history に保存する。

### 2. Contact prediction error

action-conditioned に「次の contact」を予測し、次 step の実 contact と比較する。

期待するログ:

- predicted next contact
- actual contact
- `contact_prediction_error_energy`
- `contact_prediction_error_thermal`
- `contact_prediction_error_toxicity`
- `contact_prediction_error_niche`
- `contact_prediction_error_abs_mean`

狙い:

- 外部環境を単なる field ではなく、予測と誤差を持つ対象にする。
- Deep research で確認した「precision は prediction error に対する gain として使うべき」という要件に近づける。
- 将来 TRM-Wp / TRM-As / TRM-Vm に渡す action-conditioned dataset の質を上げる。

### 3. Policy への利用

初期実装では、spatial memory と contact prediction error を軽く policy score に入れる。

例:

- 過去に hazard が高かった場所を避ける。
- 過去に energy / niche が良かった場所を選びやすくする。
- prediction error が大きい時に reconfigure / withdraw / explore などの選択を変える。

ただし、この段階では過剰に賢い world model は作らない。既存 runtime の構造に沿った軽量な shaping に留める。

## まだ未着手または要注意

- spatial memory 実装はまだ入っていない。
- contact prediction error 実装はまだ入っていない。
- `live adaptive controller` は本格実装前。
- 外部環境の時間発展はまだ単純。
- interface 経由で現実の外部世界に接続する設計は未決。
- Lenia parameter optimization は動き始めているが、十分な探索戦略・評価関数・世代継承の長期検証は未完。
- TRM 学習本体は dry run 段階で、十分な実学習・評価・promotion gate までは未完。

## 次セッションの推奨開始手順

まず現在の差分を壊さず確認する。

```bash
git status --short
rg -n "spatial_memory|contact_prediction|AdaptiveController|GenerationHarness|environment_regimes" trm_pipeline tests README.md
```

次に `trm_pipeline/erie_runtime.py` を中心に、spatial memory と contact prediction error を追加する。

確認対象:

- `RuntimeConfig`
- `ERIERuntime.__init__`
- `_contact_stats`
- `_policy_scores`
- `_single_step_policy_terms`
- `step`
- `snapshot`
- `run_episode`
- `tests/test_erie_runtime.py`

実装後の最小検証:

```bash
./.venv/bin/python -m py_compile trm_pipeline/erie_runtime.py
./.venv/bin/python -m pytest tests/test_erie_runtime.py tests/test_runtime_metrics.py
```

余裕があれば generation 系も確認する。

```bash
./.venv/bin/python -m pytest \
  tests/test_adaptive_controller.py \
  tests/test_environment_curriculum.py \
  tests/test_generation_harness.py
```

## 作業上の注意

- worktree はかなり dirty。既存の変更はユーザーまたは前回作業の成果として扱い、勝手に revert しない。
- `trm_pipeline/adaptive_controller.py`, `trm_pipeline/generation_harness.py`, `trm_pipeline/environment_curriculum.py` などは未追跡扱いの可能性があるが、前回までの実装成果なので消さない。
- `resource / hazard / shelter` は削除しない。互換 alias として残す。
- 新しい説明・新しい gate・新しい artifact では正規語彙を優先する。
- Lenia を autopoiesis そのものとして扱わない。Lenia は TRM の事前学習・外部世界関与のための基盤として扱う。
